# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# class                 border  backgr. text    indicator
client.focused          #2d2d2d #2d2d2d #009999 #2e9ef4
client.focused_inactive #2d2d2d #2d2d2d #ffffff #484e50
client.unfocused        #242424 #242424 #888888 #292d2e
client.urgent           #242424 #900000 #ffffff #900000

set $mod Mod4

font xft:Ubuntu,FontAwesome 10

floating_modifier $mod

# workspace layout
workspace_layout tabbed

bindsym $mod+Return exec xfce4-terminal -e tmux
bindsym $mod+b exec firefox -no-remote -p default
bindsym $mod+n exec nautilus --no-desktop

# kill focused window
bindsym $mod+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# Move workspace to different monitor
bindsym $mod+Mod1+Left move workspace to output left
bindsym $mod+Mod1+Right move workspace to output right

# Switch workspaces
bindsym $mod+Control+Left workspace prev
bindsym $mod+Control+Right workspace next
bindsym $mod+space workspace back_and_forth

# Alt Tab behavior
bindsym Mod1+Tab focus right
bindsym Mod1+Shift+Tab focus left

# Move container around
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+Mod1+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindcode $mod+c focus child

# switch to workspace
bindsym $mod+1 workspace 1 im
bindsym $mod+2 workspace 2 www
bindsym $mod+3 workspace 3 code
bindsym $mod+4 workspace 4 music & video
bindsym $mod+5 workspace 5 temp
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+exclam move workspace 1 im; workspace 1 im
bindsym $mod+Shift+quotedbl move workspace 2 www; workspace 2 www
bindsym $mod+Shift+periodcentered move workspace 3 code; workspace 3 code
bindsym $mod+Shift+dollar move workspace 4 music & video; workspace 4 music & video
bindsym $mod+Shift+percent move workspace 5 temp; workspace 5 temp;
bindsym $mod+Shift+ampersand move workspace 6; workspace 6
bindsym $mod+Shift+slash move workspace 7; workspace 7
bindsym $mod+Shift+parenleft move workspace 8; workspace 8
bindsym $mod+Shift+parenright move workspace 9; workspace 9
bindsym $mod+Shift+equal move workspace 10; workspace 10

# reload the configuration file
bindsym $mod+Shift+C reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R restart

set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Shift+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id ~/.i3/i3exit lock, mode "default"
        bindsym e exec --no-startup-id ~/.i3/i3exit logout, mode "default"
        bindsym s exec --no-startup-id ~/.i3/i3exit suspend, mode "default"
        bindsym h exec --no-startup-id ~/.i3/i3exit hibernate, mode "default"
        bindsym r exec --no-startup-id ~/.i3/i3exit reboot, mode "default"
        bindsym Shift+s exec --no-startup-id ~/.i3/i3exit shutdown, mode "default"
# back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
bindsym $mod+Shift+e mode "$mode_system"

# resize window (you can also use the mouse for that)
mode "resize" {

    bindsym j resize shrink left 10 px or 10 ppt
        bindsym Shift+J resize grow   left 10 px or 10 ppt

        bindsym k resize shrink down 10 px or 10 ppt
        bindsym Shift+K resize grow   down 10 px or 10 ppt

        bindsym l resize shrink up 10 px or 10 ppt
        bindsym Shift+L resize grow   up 10 px or 10 ppt

        bindsym ntilde resize shrink right 10 px or 10 ppt
        bindsym Shift+Ntilde resize grow   right 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink left 10 px or 10 ppt
        bindsym Shift+Left resize grow   left 10 px or 10 ppt

        bindsym Down resize shrink down 10 px or 10 ppt
        bindsym Shift+Down resize grow   down 10 px or 10 ppt

        bindsym Up resize shrink up 10 px or 10 ppt
        bindsym Shift+Up resize grow   up 10 px or 10 ppt

        bindsym Right resize shrink right 10 px or 10 ppt
        bindsym Shift+Right resize grow   right 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
    position bottom
    status_command i3blocks
    colors{
        background #242424
        focused_workspace #0D5a7a #0D5a7a #fdf6e3 #0D5a7a
        inactive_workspace #073642 #073642 #eee8d5 #6c71c4
        statusline #d64937
        }
}

exec urxvtd -q -o -f;urxvtc -name urxvt_python -e ipython;urxvtc -name master_volume -e alsamixer; urxvtc -name urxvt_zsh -e tmux

exec --no-startup-id xrdb -merge ~/.Xresources
exec --no-startup-id compton
exec --no-startup-id xfce4-notes
exec --no-startup-id unity-settings-daemon
exec --no-startup-id ~/.i3/conkyblocks
exec --no-startup-id synapse -s
exec --no-startup-id nm-applet
exec --no-startup-id nice -n 10 dropbox start
exec --no-startup-id setxkbmap es -option terminate:ctrl_alt_bksp -option ctrl:nocaps
exec --no-startup-id xcalc
# Comment udiskie because is causing problems with docker
#exec --no-startup-id udiskie

exec --no-startup-id feh --bg-scale ~/Dropbox/Common/wallpaper.jpg

for_window [instance="urxvt_python"] move to scratchpad, border 1pixel
for_window [instance="urxvt_zsh"] move to scratchpad, border 1pixel
for_window [instance="master_volume"] move to scratchpad border 1pixel
for_window [class="XCalc"] move to scratchpad border 1pixel
for_window [class="Xfce4-notes"] move to scratchpad border 1pixel
for_window [instance="synapse"] border none

exec --no-startup-id urxvtc -name urxvt_python -e ipython
exec --no-startup-id urxvtc -name master_volume -e alsamixer

bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show

bindsym F7 [instance="urxvt_python"] scratchpad show
bindsym F8 [instance="urxvt_zsh"] scratchpad show
bindsym F9 [instance="master_volume"] scratchpad show
bindsym F10 [class="XCalc"] scratchpad show
bindsym F12 [class="Xfce4-notes"] scratchpad show

# xfce4-notify
for_window [class="Xfce4-notes"] floating enable;border none; focus mode_toggle
for_window [class="Xfce4-notifyd"] floating enable;border none; focus mode_toggle
bindsym $mod+l exec ~/.i3/lockpanta
#bindsym $mod+less exec ~/.i3/reset_readmessages.py
bindsym Control+Tab exec i3-input -F 'exec ~/.i3/i3-wrapper.sh "%s"' -P Atajo:\  -f xft:Noto Sans 24

# Key Volume
bindsym XF86AudioRaiseVolume exec amixer -q set Master 4%+ unmute
bindsym XF86AudioLowerVolume exec amixer -q set Master 50%- unmute
bindsym XF86AudioMute exec amixer -q set Master toggle
