# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# class                 border  backgr. text    indicator
client.focused          #2d2d2d #2d2d2d #D64937 #2e9ef4
client.focused_inactive #2d2d2d #2d2d2d #ffffff #484e50
client.unfocused        #2d2d2d #2d2d2d #888888 #292d2e
client.urgent           #2f343a #900000 #ffffff #900000

set $mod Mod4

# font for window titles. ISO 10646 = Unicode
#font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1

#font xft:Droid Sans 10
font xft:Noto Sans 9

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec terminator
bindsym $mod+b exec firefox
bindsym $mod+Shift+n exec nautilus
bindsym $mod+x exec everpad --create

#bindsym $mod+F2 exec trayer --edge bottom --align right --SetDockType true --SetPartialStrut true --expand false --width 10 --height 17 --transparent true --tint 0x000000 --alpha 0
#bindsym $mod+F3 exec killall trayer


# kill focused window
bindsym $mod+q kill

# start dmenu (a program launcher)
#bindsym $mod+d exec dmenu_run

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, default)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout default

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindcode $mod+c focus child

# switch to workspace
bindsym $mod+1 workspace 1 www
bindsym $mod+2 workspace 2 code
bindsym $mod+3 workspace 3 irc
bindsym $mod+4 workspace 4 multimedia
bindsym $mod+5 workspace 5 buffer
bindsym $mod+6 workspace 6
bindsym $mod+7 workspace 7
bindsym $mod+8 workspace 8
bindsym $mod+9 workspace 9
bindsym $mod+0 workspace 10

# move focused container to workspace
bindsym $mod+Shift+exclam move workspace 1 www
bindsym $mod+Shift+quotedbl move workspace 2 code
bindsym $mod+Shift+periodcentered move workspace 3 irc
bindsym $mod+Shift+dollar move workspace 4 multimedia
bindsym $mod+Shift+percent move workspace 5 buffer
bindsym $mod+Shift+ampersand move workspace 6
bindsym $mod+Shift+slash move workspace 7
bindsym $mod+Shift+parenleft move workspace 8
bindsym $mod+Shift+parenright move workspace 9
bindsym $mod+Shift+equal move workspace 10

# reload the configuration file
bindsym $mod+Shift+C reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+R restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+E exit

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # They resize the border in the direction you pressed, e.g.
        # when pressing left, the window is resized so that it has
        # more space on its left

bindsym j resize shrink left 10 px or 10 ppt
bindsym Shift+J resize grow   left 10 px or 10 ppt

bindsym k resize shrink down 10 px or 10 ppt
bindsym Shift+K resize grow   down 10 px or 10 ppt

bindsym l resize shrink up 10 px or 10 ppt
bindsym Shift+L resize grow   up 10 px or 10 ppt

bindsym ntilde resize shrink right 10 px or 10 ppt
bindsym Shift+Ntilde resize grow   right 10 px or 10 ppt

        # same bindings, but for the arrow keys
bindsym Left resize shrink left 10 px or 10 ppt
bindsym Shift+Left resize grow   left 10 px or 10 ppt

bindsym Down resize shrink down 10 px or 10 ppt
bindsym Shift+Down resize grow   down 10 px or 10 ppt

bindsym Up resize shrink up 10 px or 10 ppt
bindsym Shift+Up resize grow   up 10 px or 10 ppt

bindsym Right resize shrink right 10 px or 10 ppt
bindsym Shift+Right resize grow   right 10 px or 10 ppt

        # back to normal: Enter or Escape
bindsym Return mode "default"
bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        position bottom
        status_command ~/.i3/conky

        colors{
        background #222222
        focused_workspace #D64937 #222222 #D64937
        inactive_workspace #dddddd #222222 #dddddd
        statusline #D64937
}
}

exec --no-startup-id compton
exec --no-startup-id gnome-settings-daemon
# Start synapse in startup mode
exec --no-startup-id synapse -s
exec --no-startup-id nm-applet
exec --no-startup-id dropbox start
exec --no-startup-id i3-msg 'workspace 4;exec --no-startup-id everpad;workspace 1 www'
exec --no-startup-id setxkbmap es -option terminate:ctrl_alt_bksp -option ctrl:nocaps

# This is my note # 63 wich holds all the shortcuts I use
#exec --no-startup-id everpad --open 63

# This is my note # 65 a scratchpad for day to day use
#exec --no-startup-id everpad --open 65
#exec unclutter
#set $font Ubuntu-10
#set $selbgcolor #DD4814
#set $dmc $(</tmp/dmenu.conf)
#exec --no-startup-id echo '-i -fn $font -sb $selbgcolor' > /tmp/dmenu.conf
#bindsym $mod+d exec dmenu_run $dmc
#bindsym $mod+d exec ~/.i3/dropdown_dmenu.py
bindsym $mod+d exec ~/.i3/everpad-note-launch/everpad_note_launch.py
#bindsym $mod+m exec --no-startup-id i3-dmenu-desktop --dmenu="/usr/local/bin/dmenu $dmc"

#exec --no-startup-id xsetroot -solid "#111111"

for_window [instance="urxvt_python"] move to scratchpad, border 1pixel
for_window [instance="master_volume"] move to scratchpad border 1pixel
for_window [instance="urxvt_mc"] move to scratchpad border 1pixel
for_window [instance="urxvt_zsh"] move to scratchpad border 1pixel
#for_window [instance="urxvt_vimscratchpad"] move to scratchpad border 1pixel
for_window [instance="synapse"] border none
#for_window [title="Everpad / Shortcuts que no recuerdo tanto"] move to scratchpad border 1 pixel
#for_window [title="Everpad / Scratchpad del dia a dia"] move to scratchpad border 1 pixel

exec --no-startup-id urxvt -name urxvt_python -e ipython
exec --no-startup-id urxvt -name master_volume -e alsamixer
exec --no-startup-id urxvt -name urxvt_mc -e mc
exec --no-startup-id urxvt -name urxvt_zsh
#exec --no-startup-id urxvt -name urxvt_vimscratchpad -e vim ~/scratchpad
# I have to use trayer as a suplement because i3bar tray does not behave correctly
#exec --no-startup-id trayer --edge bottom --align right --SetDockType true --SetPartialStrut true --expand false --width 10 --height 17 --transparent true --tint 0x000000 --alpha 0
#exec --no-startup-id wingpanel


bindsym $mod+Shift+minus move scratchpad
bindsym $mod+minus scratchpad show

bindsym $mod+i [instance="urxvt_python"] scratchpad show
bindsym $mod+n [instance="urxvt_mc"] scratchpad show
bindsym $mod+Shift+v [instance="master_volume"] scratchpad show
#bindsym $mod+F1 [title="Everpad / Shortcuts que no recuerdo tanto"] scratchpad show
#bindsym $mod+less [instance="urxvt_vimscratchpad"] scratchpad show 
bindsym $mod+z [instance="urxvt_zsh"] scratchpad show

# xfce4-notify
for_window [class="Xfce4-notifyd"] floating enable;border none; focus mode_toggle

bindsym $mod+l exec ~/.i3/lockpanta

bindsym $mod+less exec quickswitch.py

bindsym    Control+Tab exec i3-input -F 'exec ~/.i3/i3-wrapper.sh "%s"' -P Atajo:\  -f xft:Noto Sans 24

